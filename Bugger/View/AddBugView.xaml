<UserControl x:Class="Bugger.View.AddBugView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Bugger.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0, 30, 0, 0">
            <TextBox Width="50" Height="auto" Text="{Binding Status, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>
        
        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,30,0,0">
            <TextBox Width="50" Height="auto" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Margin="5,0,0,0" Content="Submit" Command="{Binding AddBugCommand}"/>
            <Button Margin="5,0,0,0" Content="Cancel" Command="{Binding CancelAddBugCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
